apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kamer-pro-chart.fullname" . }}-frontend
  labels:
    {{- include "kamer-pro-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
data:
  REACT_APP_API_URL: "http://{{ include "kamer-pro-chart.fullname" . }}-backend:8000"